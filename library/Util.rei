/** Returns a colorful "Hello, World" message.

    {4 Examples}

    {[
    print_endline(hello());
    ]} */
let hello: unit => string;

/** Get the input for a given problem. */
let getProblemInput: int => string;